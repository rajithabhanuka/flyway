CREATE TABLE `users_entity` (
                                `user_id` int(11) NOT NULL AUTO_INCREMENT,
                                `email_type` int(11) DEFAULT NULL,
                                PRIMARY KEY (`user_id`)
)
